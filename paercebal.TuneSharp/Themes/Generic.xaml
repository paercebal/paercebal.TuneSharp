<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:paercebal.TuneSharp"
    xmlns:custom="clr-namespace:paercebal.TuneSharp.CustomControls">


    <Style TargetType="{x:Type custom:ClickableProgressBar}"
           BasedOn = "{StaticResource {x:Type ProgressBar}}">
        <!--
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ClickableProgressBar}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        -->
    </Style>
</ResourceDictionary>
