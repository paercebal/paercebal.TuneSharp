<Window x:Class="paercebal.TuneSharp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:paercebal.TuneSharp"
        xmlns:custom="clr-namespace:paercebal.TuneSharp.CustomControls"
        mc:Ignorable="d"
        Title="paercebal.TuneSharp" Height="200" Width="800"
        Background="#88CCFF"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="25"/>
            </Grid.ColumnDefinitions>
            <custom:MusicPlayer Grid.Column="0" x:Name="musicPlayer" />
            <Button x:Name="OpenFileButton" Grid.Column="1" Margin="1" Click="OnOpenFileButtonClick" Content="..." ToolTip="Open File" />
        </Grid>

        <TextBox Grid.Row="1" x:Name="DebugTextBox" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Visible"/>

    </Grid>
</Window>
